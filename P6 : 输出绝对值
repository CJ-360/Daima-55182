#include <iostream>
#include <iomanip>
#include <cmath>

int main() {
    double n;

    std::cin >> n;

    double result = std::abs(n);

    std::cout << std::fixed << std::setprecision(2) << result << std::endl;

    return 0;
}
