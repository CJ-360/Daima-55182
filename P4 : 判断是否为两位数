#include <iostream>

int main() {
    int n;
    // 读取输入
    std::cin >> n;

    // 判断是否同时满足大于等于10 和 小于等于99
    if (n >= 10 && n <= 99) {
        std::cout << 1 << std::endl;
    } else {
        std::cout << 0 << std::endl;
    }

    return 0;
}
