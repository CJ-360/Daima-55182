#include <iostream>

int main() {
    int n;

    std::cin >> n;

    if (n >= 10 && n <= 99) {
        std::cout << 1 << std::endl;
    } else {
        std::cout << 0 << std::endl;
    }

    return 0;
}
